<h4 class="mb-3">👤 Hồ sơ giảng viên</h4>
<form method="POST">
  <div class="mb-3">
    <label class="form-label">Tên</label>
    <input name="name" value="{{profile.name}}" class="form-control" required>
  </div>
  <div class="mb-3">
    <label class="form-label">Giới thiệu</label>
    <textarea name="bio" rows="5" class="form-control">{{profile.bio}}</textarea>
  </div>
  <button class="btn btn-primary">Lưu</button>
</form>
